echo >"0:/user/networkmode.g" "M552 I1 S1 ; Enable WiFi Client Mode"

if !exists(param.R)
    M291 R"Do you want to restart WiFi module and switch mode now?" P" " S3
    M552 I1 S-1
    G4S1
    M98 P"0:/user/networkmode.g"
    G4S1
	M98 P"0:/sys/networkmode.g"